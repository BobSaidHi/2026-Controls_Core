# Net-Application

This folder contains code that provides an TCP layer 4 (OSI layer 8) interface, the application layer.  It provides custom and protocol specific code that relies on the lower layers to transport the actual data.
