# Net-Link

This folder contains code that provides an OSI Layer two interface, the data link layer.  It provides most of the underlying features we need.  In most cases it relies on the physical layer (OSI layer 1), but some protocols bypass layering.

